# BlueWave Cleaning

A one-page site used as the live demo for the AI Strategy Exchange GitHub workshop.

The homepage headline in `index.html` is the one line we change to show how a change
moves from a branch, through review, into `main`, and out to the live site.

Live site: published from `main` via GitHub Pages.
